# Warp Router — sysctl settings for IP forwarding and security

# Enable IPv4 forwarding
net.ipv4.ip_forward = 1

# Reverse path filtering (loose mode — needed for multi-WAN ECMP)
net.ipv4.conf.default.rp_filter = 2
net.ipv4.conf.all.rp_filter = 2

# Conntrack tuning
net.netfilter.nf_conntrack_max = 262144

# TCP SYN flood protection
net.ipv4.tcp_syncookies = 1

# Disable ICMP redirects (router should not accept or send them)
net.ipv4.conf.all.accept_redirects = 0
net.ipv4.conf.default.accept_redirects = 0
net.ipv4.conf.all.send_redirects = 0
net.ipv4.conf.default.send_redirects = 0

# Ignore ICMP broadcast pings (smurf protection)
net.ipv4.icmp_echo_ignore_broadcasts = 1

# Log martian packets
net.ipv4.conf.all.log_martians = 1
net.ipv4.conf.default.log_martians = 1
